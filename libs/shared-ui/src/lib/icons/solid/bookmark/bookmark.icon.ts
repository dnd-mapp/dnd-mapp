import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
    selector: 'dma-bookmark-so-icon, dma-icon[dma-bookmark-so-icon]',
    templateUrl: './bookmark.icon.svg',
    styleUrl: '../../icons.scss',
    changeDetection: ChangeDetectionStrategy.OnPush,
})
export class BookmarkSoIcon {}
